"""Shipped schema migration files.

Each ``NNNN_description.sql`` file here is applied in order by
:func:`deeparchive.db.migrations.migrate`. Append-only: never edit a shipped
file once released; write a new one instead.
"""
